## 変更履歴

### 2026-07-28 初心者向けセットアップ導線を追加（setup.bat新設・README判断チャート・トラブルシューティング）

- 課題: README.mdの導入手順（Pythonインストール→config.py作成→LLMプロバイダ選択）が一般ユーザーには敷居が高いとの指摘。「ユーザー自身がLLM/APIを用意する」設計自体（開発者側が推論コストを負担しないための意図的設計）は維持したまま、手順の分かりやすさ・自動化のみ改善
- `setup.bat`を新規作成。`config.py`が既に存在する場合は上書きせず、存在しない場合のみ`config.example.py`からコピー→`pip install -r requirements.txt`を実行→完了メッセージ表示→`pause`で終了確認、という一連の流れをダブルクリックのみで完結させる仕様
  - 既存の`start.bat`/`起動.bat`/`弾正戦国転生記.bat`の慣習（`chcp`不使用・`@echo off`→`cd /d "%~dp0"`→`pause`）に合わせて`chcp`は使用していない。ただし既存.batはいずれもファイル内に日本語echo文を持たない前例のみだったため、UTF-8保存(Write/Edit)とWindows既定コンソール(cp932)の不一致による文字化けリスク（過去のcp932/UTF-8混在事故の教訓）を踏まえ、`setup.bat`内の表示メッセージは英語のみで構成し、文字化けリスク自体を回避する設計とした
- README.mdに「## セットアップ前に：あなたに合ったAIの選び方」セクションを新設（`## 動作環境`と`## セットアップ手順`の間）。GPU有無・無料/課金志向に応じた3択（①ローカルLLM/②Gemini API/③Claude API）の早見表と、`setup.bat`実行を促す一言を追加
  - 各選択肢から該当セクションへのリンクは、GitHubの自動アンカースラッグ生成（全角括弧を含む見出しでの挙動が不確実）に依存せず、`① ローカルLLM（...）`等の見出し直前に`<a id="local-llm-section"></a>`等の明示アンカーを追加する方式で実装（`gemini-section`/`claude-section`も同様）
- README.md末尾側、ステップ6（ゲーム起動）の後・APIキー管理セクションの前に「## トラブルシューティング（よくある質問）」を新設。`pip install`失敗時の対処（Pythonバージョン/pip更新/パス）、`'python' is not recognized`エラーの対処（PATH追加）、LMStudioサーバー未起動、APIキー未設定エラー、`setup.bat`失敗時の手動対処を掲載
- 変更範囲はREADME.md編集と`setup.bat`新規追加のみ。`config.py`/`config.example.py`の中身・`LLM_PROVIDER`のデフォルト値・既存データファイル（`hizen_1560.json`等）には一切触れていない

### 2026-07-14 転生者知識伝達モード（Kコマンド）を追加

- 評定画面に「K」コマンドを追加。主君（転生者）が現代の知識を軍師に伝えるための専用の多ターン会話ループを実装（`main.py`のaction=="k"分岐、`ui/cli.py`の`get_knowledge_input()`、`llm/warlord.py`の`chat_knowledge_transfer()`）
- 軍師側のペルソナに知識伝達モード用のプロンプトを追加（`_KNOWLEDGE_TRANSFER_ADDON`）。知らない概念は聞き返す・謙り言葉を使う・「AIとして」「転生者」等の破綻ワードを言わない、等を明記
- 評定の選択入力（`get_player_action()`）を、固定キー以外の自由文字列も受け付けるよう変更。数字・アルファベット以外の入力は軍師への直接発言として扱う
- 詳細設計は `転生者知識システム_設計メモ.md` を参照
- 未配線（準備段階）: `llm/base.py`/`llm/anthropic_llm.py`に`chat_stream()`、`ui/cli.py`に`show_advisor_stream()`を追加したが`main.py`からは未呼び出し
- 未配線（試作段階）: `agent_server.py`（ポート7654のHTTPブリッジ）・`watch_requests.py`（`llm_request.json`監視スクリプト）を追加。既存の`llm/agent_llm.py`（`claude -p`サブプロセス呼び出し）とは別方式のClaude Code連携の試作で、main.pyからは未参照

### 2026-07-14 大串浦の所有者をryuzoji→omuraに修正

- 2026-07-05の経済モデル統合時に「所有者が親城と異なる飛び地」の検証用ケースとして意図的にryuzoji領としていたが、実プレイでの動作確認中にユーザーが「大村のはず」と指摘。石高0.002万石の小規模拠点でありゲームバランスへの影響はないため、`data/scenarios/hizen_1560.json`・`map_adjacency.html`のcastlesオブジェクトの両方をomuraに修正
- 併せて、既存拠点の所有者をマップ上のUIから変更する機能は現状存在しない（拠点追加時の選択とドラッグでの位置変更のみ）ことを確認。発生頻度が低いため機能追加はせず、必要時に都度対応する方針とした

### 2026-07-13 マップ・ポップアップに規模ランク表示を追加（15段階ティア化）

- 2026-07-05の経済モデル統合で導入した`compute_total_koku()`（親城＋配下の村・港の合計石高）を使い、マップのツールチップ・ポップアップに「規模Lv.1〜15」を追加表示。既存の石高表示（自分単独の`koku`）はそのまま残し、規模ランクは追記のみ
- `engine/game_state.py`にモジュールレベルの`SCALE_RANK_THRESHOLDS`（万石の下限値リスト）と`scale_rank(total_koku)`を追加。Lv.15は「100万石〜」で上限なし（複数国統一等でさらに大規模化する場合はリストに行を追加するだけで対応可能）
- `write_map_state()`が`total_koku`・`rank`フィールドを`map_state.json`に追加出力するよう変更（既存の`koku`フィールドは変更なし）
- `map_adjacency.html`の`applyMapState()`（ライブ更新側）にランク表示を追加。初期描画（ゲーム起動直後の静的表示）は非対応のまま据え置き、既存の「兵力」表示と同様に最初のポーリング（2.5秒後）で表示される仕様
- ランクは表示専用の派生値でありデータファイルには保存しない。ターン経過で石高・従属関係が変わるたびに自動で再計算される
- スコープ外: 耐久度（`Territory.fortification`、防御力0〜5）との連動は今回対象外。既存の別軸フィールドであり規模ランクとは独立

### 2026-07-05 城の石高を「自分の直轄分＋配下の村・港の合計」に変更（経済モデル統合）

- 設計変更: 城は`koku`を単独で持つのではなく、`parent_castle`で紐づく配下の村・港（～浦・～津などの港も含む）の石高を積み上げた合計を「規模」として扱う方針に変更。将来的に全城が村単位まで分解されればoriginal own_koku=0でも成立する積み上げ式
- `engine/game_state.py`に`GameState.compute_total_koku()`（parent_castleチェーンを再帰的に積み上げる）と`GameState.is_absorbed_child()`（親城と所有者が一致する場合のみ「吸収される子」とみなす）を追加
- `_calc_income()`・`advance_turn()`の兵糧収入計算を、親城に吸収される村・港は単独計上せず親城側の合計に含める形に変更（二重計上防止）。商業収入・港格収入・産業収入は従来通り領地ごとに個別計上（koku以外の収入源は変更なし）
- 所有者が親城と異なる飛び地（例: 大串浦をryuzojiが領有、所属先の八幡山城はomura領）は「吸収される子」とみなさず、独立した経済単位として扱う（他勢力に奪われた飛び地が奪取先の収入に化けるバグを回避）
- 人口・最大兵力・兵力自然回復・AIの内政対象選択は今まで通り各領地の直轄石高（own koku）ベースのまま変更なし（軍事・人口は村単位で現状維持という方針のため）
- 検証: 12ターン分の`advance_turn()`をエラーなく実行、クロスオーナーの飛び地ケース（大串浦）で二重計上・誤帰属が無いことを確認済み
- 未着手: マップ・ポップアップへの規模ランク表示、10〜20段階の規模ティア化、耐久度（別軸）との連動は次のステップとして保留

### 2026-07-05 城ポップアップの石高表示を統一（色分け廃止）

- 不具合: 同じ城のポップアップでも、地図起動直後（初期描画・静的値）は石高が青字（`#2980b9`）、`map_state.json`ポーリング後（ライブ値）はグレー字になっており、由来が違うだけで紛らわしいとの指摘
- `map_adjacency.html`の初期ポップアップ生成部分から青字指定を削除し、ライブ更新側と同じプレーン表示に統一

### 2026-07-05 地図の拠点追加を「本物の城」として自動反映する仕組みを追加

- 不具合: 地図（map_adjacency.html）で「拠点追加」した城が localStorage にしか保存されず、`hizen_1560.json`/`game_state.py`のゲームデータに一切反映されていなかった（`main.py`のローカルサーバーは`SimpleHTTPRequestHandler`で読み取り専用・書き込み経路が存在しなかった）
- ユーザーが手入力していた7件の拠点（天久保村・面高村・八木原村・八幡山城・大串浦・大串村・鳥加村）と、既存城20件分の座標修正（藤原権現山城・杠城・熊の川城・畑瀬城・谷田城・古湯城・佐志方城・牛ノ岳城・口之津・横瀬浦・河内浦・肥前天狗山城・中浦城・七ツ釜浦・下り山城・肥前黒瀬城・鳥越城・神浦城・肥前田中城・肥前天崎城・肥前岳城）を、地図の「座標出力」エクスポートを元に`hizen_1560.json`・`engine/game_state.py`のADJACENCY・`map_adjacency.html`のcastlesオブジェクトへ反映
- 新規7件の石高・兵力は近隣の同規模拠点（村0.001万石/3兵、港0.002万石/8兵、城0.003万石/10兵）を基準に暫定値を設定。隣接関係は各拠点の座標から所属城への直線距離＋（所属城が遠い場合は）最短距離拠点への橋渡し接続で自動算出
- 恒久対応: `main.py`に`/api/save_map`のPOSTエンドポイントを追加。地図で拠点追加・ドラッグ移動するたびに`map_adjacency.html`が自動でPOSTし、新規拠点は`hizen_1560.json`へ追記、隣接関係は新規ファイル`data/scenarios/custom_adjacency.json`へ追記（`game_state.py`起動時に`ADJACENCY`へ自動マージ）、座標は`map_adjacency.html`のcastlesオブジェクトへ直接書き戻される
- 注意: ゲームは起動時に一度シナリオを読み込むため、実行中のセッションには即時反映されない（従来通り再起動が必要）。石高・兵力・隣接関係は暫定値のため、必要に応じて個別調整が必要
- 影響ファイル: `main.py`（`_MapRequestHandler`/`_save_map_edit`等を追加）、`engine/game_state.py`（`_merge_custom_adjacency()`追加）、`map_adjacency.html`（`syncCastlesToServer()`追加、拠点追加・ドラッグ時に呼び出し）、`data/scenarios/hizen_1560.json`（7件追加）

### 2026-06-19 agentモード追加

- `llm/agent_llm.py` を新規作成
  - `BaseLLM` を実装するファイルポーリング型LLM
  - `chat()` が `llm_request.json` にプロンプトを書き出し、`llm_response.json` が現れるまで最大300秒ポーリング（1秒間隔）
  - レスポンス受信後に両ファイルを削除して続行。タイムアウト時は空文字返し（既存のフォールバック処理が対応）
- `config.py`: `LLM_PROVIDER` のコメントに `"agent"` を追記
- `main.py`: `_pick_llm()` に `"5": "agent"` を追加（起動時UIで `5:Agent（ファイル経由）` を選択可能）

#### agentモードの使い方
1. ゲーム起動時のLLM選択で `5` を入力
2. LLMが必要になると `弾正戦国転生記/llm_request.json` が生成される
3. Claude Code（マスターエージェント）が `llm_request.json` を読み、`llm_response.json` に `{"response": "..."}` 形式で書く
4. ゲームが自動的に読み取り、両ファイルを削除して続行

